#!/usr/bin/env bash
#
# Run the unit test suite. Extra arguments pass through to rake.
#
#   bin/test                     the whole suite
#   bin/test TEST=test/foo_test.rb   a single file
#
set -euo pipefail
cd "$(dirname "$0")/.."
exec bundle exec rake test "$@"
